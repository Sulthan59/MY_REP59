version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('DB_RAW')}}"
      schema: DB_SCHEMA_RAW
      tables:
        - name: RAW_CUSTOMERS_DATA
        - name: RAW_ORDERS_DATA
        - name: EMPLOYEES_RAW
